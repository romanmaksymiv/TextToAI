<Window x:Class="TextToAI.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TextToAI Settings"
        Height="450" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- API Key -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="OpenAI API Key" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <PasswordBox x:Name="ApiKeyPasswordBox" Grid.Column="0" Padding="5"/>
                <TextBox x:Name="ApiKeyTextBox" Grid.Column="0" Padding="5" Visibility="Collapsed"/>
                <Button x:Name="ToggleApiKeyButton" Grid.Column="1" Content="Show" Width="50" Margin="5,0,0,0" Click="ToggleApiKeyVisibility_Click"/>
            </Grid>
        </StackPanel>

        <!-- Model -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Model" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox x:Name="ModelComboBox" Padding="5">
                <ComboBoxItem Content="gpt-4o" IsSelected="True"/>
                <ComboBoxItem Content="gpt-4o-mini"/>
                <ComboBoxItem Content="gpt-4-turbo"/>
                <ComboBoxItem Content="gpt-3.5-turbo"/>
            </ComboBox>
        </StackPanel>

        <!-- Hotkey -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Hotkey" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="HotkeyTextBox" Grid.Column="0" Padding="5" IsReadOnly="True"/>
                <Button x:Name="RecordHotkeyButton" Grid.Column="1" Content="Record" Width="60" Margin="5,0,0,0" Click="RecordHotkey_Click"/>
            </Grid>
        </StackPanel>

        <!-- Prompt -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Prompt (use {text} as placeholder)" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="PromptTextBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Height="120"
                     Padding="5"/>
        </StackPanel>

        <!-- Start with Windows -->
        <CheckBox x:Name="StartWithWindowsCheckBox" Grid.Row="4"
                  Content="Start with Windows" Margin="0,0,0,15"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Save" Width="80" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
