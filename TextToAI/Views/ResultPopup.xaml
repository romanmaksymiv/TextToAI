<Window x:Class="TextToAI.Views.ResultPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TextToAI"
        Height="300" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        Topmost="True"
        ShowInTaskbar="False"
        KeyDown="Window_KeyDown">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Loading state - spinning circle -->
        <Grid x:Name="LoadingPanel" Grid.Row="0"
              VerticalAlignment="Center" HorizontalAlignment="Center">
            <Ellipse Width="40" Height="40"
                     Stroke="#E0E0E0" StrokeThickness="4"/>
            <Ellipse x:Name="SpinnerArc" Width="40" Height="40"
                     Stroke="#0078D4" StrokeThickness="4"
                     StrokeDashArray="2 4"
                     RenderTransformOrigin="0.5,0.5">
                <Ellipse.RenderTransform>
                    <RotateTransform x:Name="SpinnerRotation"/>
                </Ellipse.RenderTransform>
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation
                                    Storyboard.TargetName="SpinnerRotation"
                                    Storyboard.TargetProperty="Angle"
                                    From="0" To="360" Duration="0:0:1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
            </Ellipse>
        </Grid>

        <!-- Result text -->
        <TextBox x:Name="ResultTextBox" Grid.Row="0"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Padding="10"
                 FontSize="13"
                 Visibility="Collapsed"/>

        <!-- Buttons and duration -->
        <Grid x:Name="ButtonPanel" Grid.Row="1" Margin="0,15,0,0" Visibility="Collapsed">
            <TextBlock x:Name="DurationText"
                       Text=""
                       Foreground="Gray"
                       FontSize="11"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CopyButton" Content="Copy" Width="80" Margin="0,0,10,0" Click="Copy_Click"/>
                <Button x:Name="DismissButton" Content="Dismiss" Width="80" Click="Dismiss_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
